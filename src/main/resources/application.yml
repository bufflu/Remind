# DataSource Config
spring:
  datasource:
    driver-class-name: org.h2.Driver
    schema: classpath:db/schema-h2.sql
    data: classpath:db/data-h2.sql
    url: jdbc:h2:mem:test
    username: root
    password: test


mybatis-plus:
  global-config:
    db-config:
      id-type: input

server:
  port: 10000

justauth:
  enabled: true
  type:
    GITHUB:
      clientId: 81ca1fa6b5a*******
      clientSecret: 1b9261eea907a5cd344*************
      redirectUri: http://127.0.0.1:10000/oauth/callback/github



